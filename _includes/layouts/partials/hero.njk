{% set modifiers = '' %}
{% if modifiers %}
  {% for mod in modifiers %}
    {% set modifiers = modifiers + ' c-hero--'+mod %}
  {% endfor %}
{% endif %}

{% if img %}
  {% set modifiers = modifiers + ' c-hero--bg-img' %}
{% endif %}

<section class="c-hero {{ modifiers }} {% if quote %}c-hero--quote{% endif %}">
  {% if img %}
  <div class="c-hero__img" style="--hero-image: url('{{ img }}');"></div>
  {% endif %}
    <div class="l-main">
      {% set heroTitle = renderData.title or title %}
      {% if quote %}
      <blockquote class="c-hero__blockquote">
        <span class="c-hero__quote">{{ quote | safe }}</span>
        {% if quoteAuthor %}
        <cite class="c-hero__subtitle">{{ quoteAuthor | safe }}</cite>
        {% endif %}
      </blockquote>
      {% else %}
        <h1 class="c-hero__title">{{ heroTitle | safe }}</h1>
      {% endif %}
      {% if subtitle %}
      <h2 class="c-hero__subtitle">{{ subtitle | safe }}</h2>
      {% endif %}
    </div>
    {# {% if imgAuthorName %}
      <span class="c-hero__credits">photo by <a href="{{ imgAuthorProfile }}" target="_blank" rel="noopener">{{ imgAuthorName }}</a> on <a href="{{ img }}" target="_blank" rel="noopener">Unsplash</a></span>
    {% endif %} #}
</section>
